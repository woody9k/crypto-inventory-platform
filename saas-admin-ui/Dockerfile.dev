FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci || npm install

COPY . .

# Expose 3002 for the SaaS Admin UI dev server
EXPOSE 3002

# Run Vite on 0.0.0.0 and port 3002
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3002"]


